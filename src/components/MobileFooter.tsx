
// Este componente foi substituído pelas funcionalidades de navegação no Header.tsx
// Mantemos como um componente vazio para evitar quebrar importações existentes

import { useLocation } from "react-router-dom";

export const MobileFooter = () => {
  return null;
};

export default MobileFooter;
